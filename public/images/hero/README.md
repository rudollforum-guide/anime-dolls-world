# Hero image

`anime-maids.webp` is the single active hero image. Its crop is controlled in
`app/globals.css`: desktop keeps the original right-weighted composition, while
mobile focuses the image at 68% horizontally to keep the main character's face visible.
